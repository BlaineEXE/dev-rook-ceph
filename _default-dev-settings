#!/usr/bin/env bash

export NODE_COUNT=1

export NODE_CPUS=5
export NODE_MEMORY_GB=10

export NODE_DISK_COUNT=3
# export NODE_DISK_SIZE_GB=20

# You can control minikube flags with env vars preceded by MINIKUBE_ also
# export MINIKUBE_CNI=flannel
# export MINIKUBE_CONTAINER_RUNTIME='cri-o'

# containerd is reportedly the least resource intensive runtime
export MINIKUBE_CONTAINER_RUNTIME='containerd'
