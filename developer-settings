#!/usr/bin/env bash

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
# shellcheck source=_default-dev-settings
source "${SCRIPT_DIR}/_default-dev-settings"

# Add your own overrides to the default-dev-settings here
# examples:

# export NODE_COUNT=2

# export MASTER_CPUS=2
# export MASTER_MEMORY_SIZE_GB=2

# export NODE_CPUS=2
# export NODE_MEMORY_SIZE_GB=2

# export DISK_COUNT=2
# export DISK_SIZE_GB=10
